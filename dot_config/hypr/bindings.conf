# Application bindings
$terminal = uwsm app -- alacritty
$browser = uwsm app -- chromium --new-window --ozone-platform=wayland
$webapp = $browser --app

bindd = SUPER, backspace, Terminal, exec, $terminal
bindd = SUPER, F, File manager, exec, uwsm app -- nautilus --new-window
# bindd = SUPER, B, Browser, exec, $browser
bind = SUPER, B, exec, uwsm app -- zen-browser --ozone-platform=wayland
bindd = SUPER, M, Music, exec, uwsm app -- spotify
bindd = SUPER, N, Neovim, exec, $terminal -e nvim
bindd = SUPER, T, Activity, exec, $terminal -e btop
bindd = SUPER, D, Docker, exec, $terminal -e lazydocker
bindd = SUPER, G, Signal, exec, uwsm app -- signal-desktop
bindd = SUPER, O, Obsidian, exec, uwsm app -- obsidian -disable-gpu
# bindd = SUPER, slash, Passwords, exec, uwsm app -- 1password

bindd = SUPER, A, ChatGPT, exec, $webapp="https://chatgpt.com"
bindd = SUPER SHIFT, A, Grok, exec, $webapp="https://grok.com"
bindd = SUPER, C, Calendar, exec, $webapp="https://app.hey.com/calendar/weeks/"
bindd = SUPER, E, Email, exec, $webapp="https://app.hey.com"
bindd = SUPER, Y, YouTube, exec, $webapp="https://youtube.com/"
bindd = SUPER SHIFT, G, WhatsApp, exec, $webapp="https://web.whatsapp.com/"
bindd = SUPER ALT, G, Google Messages, exec, $webapp="https://messages.google.com/web/conversations"
bindd = SUPER, X, X, exec, $webapp="https://x.com/"
bindd = SUPER SHIFT, X, X Post, exec, $webapp="https://x.com/compose/post"

# -----------------------------------------------------
# Custom Vim-style bindings
# -----------------------------------------------------

# --- Unbind default keys to avoid conflicts ---
# We are unbinding the original keys from Omarchy's config.
# This ensures they don't interfere with our new hjkl bindings.
unbind = SUPER, J
unbind = SUPER, K
unbind = SUPER, SPACE

# --- Main hjkl navigation ---
# Focus movement
bind = SUPER, h, movefocus, l
bind = SUPER, l, movefocus, r
bind = SUPER, k, movefocus, u
bind = SUPER, j, movefocus, d

# Window swapping
bind = SUPER SHIFT, h, swapwindow, l
bind = SUPER SHIFT, l, swapwindow, r
bind = SUPER SHIFT, k, swapwindow, u
bind = SUPER SHIFT, j, swapwindow, d

# --- Re-assign original functions to new keys ---
# We're moving the original functions of Super+J and Super+K
# to the keys you requested.
bind = SUPER, Tab, togglesplit, # Previously Super+J
# bind = SUPER, slash, exec, pkill wofi || ~/.local/bin/custom-keybindings-menu # Previously Super+K
bind = SUPER, slash, exec, pkill wofi || ~/.local/share/omarchy/bin/omarchy-menu-keybindings # Previously Super+K

# Override Walker with Vicinae
bindd = SUPER, SPACE, Launch Vicinae, exec, vicinae
